# ============================================================================
# SISTEMA DE ADMINISTRACIÓN SEGURA - Variables de Entorno
# ============================================================================
#
# IMPORTANTE:
# 1. Copiar este archivo a .env (si no existe)
# 2. Generar ADMIN_MASTER_KEY: openssl rand -hex 32
# 3. NUNCA subir .env a Git
# 4. Hacer backup de ADMIN_MASTER_KEY en lugar seguro
#
# ============================================================================

# Clave maestra para cifrado de secrets TOTP
# Generar con: openssl rand -hex 32
# ⚠️ CRÍTICO: Sin esta clave, no se pueden descifrar los secrets TOTP
ADMIN_MASTER_KEY=

# Base de datos (si no está ya configurada)
DATABASE_URL="postgresql://usuario:password@localhost:5432/database?schema=public"

# Dominio de la aplicación
NEXT_PUBLIC_APP_URL=https://tu-dominio.com

# Puerto admin (para desarrollo local)
# En producción, NGINX escucha en 8443 y hace proxy a este puerto
ADMIN_PORT=3001
