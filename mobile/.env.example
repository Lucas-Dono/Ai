# ═══════════════════════════════════════════════════════════════════
# CONFIGURACIÓN DE API - App Móvil
# ═══════════════════════════════════════════════════════════════════

# -----------------------------------------------------------------------
# URL de API en Desarrollo
# -----------------------------------------------------------------------
# IMPORTANTE: No uses "localhost" o "127.0.0.1" - no funcionará en emuladores/dispositivos
#             Debes usar tu IP local en la red (ej: 192.168.x.x o 10.0.x.x)
#
# CÓMO OBTENER TU IP LOCAL:
#
# Linux/Mac:
#   ip addr show | grep "inet " | grep -v 127.0.0.1
#   o
#   ifconfig | grep "inet " | grep -v 127.0.0.1
#   o
#   hostname -I
#
# Windows:
#   ipconfig | findstr IPv4
#
# Ejemplo: Si tu IP es 192.168.1.150, usa:
# DEV_API_URL=http://192.168.1.150:3000
DEV_API_URL=http://192.168.0.170:3000

# -----------------------------------------------------------------------
# URL de API en Producción
# -----------------------------------------------------------------------
# Usa tu dominio de producción cuando la app esté desplegada
PROD_API_URL=https://api.example.com

# ═══════════════════════════════════════════════════════════════════
# NOTAS ADICIONALES
# ═══════════════════════════════════════════════════════════════════
#
# 1. Copia este archivo a .env y configura tus valores
# 2. Nunca subas el archivo .env al repositorio (ya está en .gitignore)
# 3. Asegúrate de que tu servidor backend esté corriendo antes de iniciar la app
# 4. Si cambias de red, deberás actualizar tu IP local
# 5. Para Push Notifications, configura también mobile/app.json (ver README.md)
